overwrite: true
schema: "http://localhost:3000/graphql" # Replace with the appropriate URL for your GraphQL API
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHOC: false
      withComponent: false
      withHooks: false`
